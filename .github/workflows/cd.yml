name: CD

on:
  push:
    branches: [main]

env:
  PROJECT_ID: infra-demo-tharsis
  REGION: southamerica-east1
  REPOSITORY: infra-demo
  SERVICE: infra-demo

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Só roda se o CI passar
    needs: []

    permissions:
      contents: read
      id-token: write  # necessário para Workload Identity Federation

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build JAR
        run: ./mvnw package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # ── Autenticação GCP ─────────────────────────────────
      # Descomenta quando o billing estiver ativo
      # - name: Authenticate to GCP
      #   uses: google-github-actions/auth@v2
      #   with:
      #     workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
      #     service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}

      # - name: Set up Cloud SDK
      #   uses: google-github-actions/setup-gcloud@v2

      # ── Build e Push para Artifact Registry ─────────────
      # - name: Configure Docker for Artifact Registry
      #   run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      # - name: Build and push Docker image
      #   uses: docker/build-push-action@v5
      #   with:
      #     context: .
      #     push: true
      #     tags: |
      #       ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:latest
      #       ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:${{ github.sha }}
      #     cache-from: type=gha
      #     cache-to: type=gha,mode=max

      # ── Terraform Apply ──────────────────────────────────
      # - name: Setup Terraform
      #   uses: hashicorp/setup-terraform@v3

      # - name: Terraform Init
      #   working-directory: terraform
      #   run: terraform init

      # - name: Terraform Apply
      #   working-directory: terraform
      #   run: |
      #     terraform apply -auto-approve \
      #       -var-file=environments/prod/terraform.tfvars \
      #       -var="image=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:${{ github.sha }}"

      # ── Deploy Cloud Run ─────────────────────────────────
      # - name: Deploy to Cloud Run
      #   uses: google-github-actions/deploy-cloudrun@v2
      #   with:
      #     service: ${{ env.SERVICE }}
      #     image: ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:${{ github.sha }}
      #     region: ${{ env.REGION }}

      # ── Smoke test ───────────────────────────────────────
      # - name: Smoke test
      #   run: |
      #     URL=$(gcloud run services describe ${{ env.SERVICE }} \
      #       --region=${{ env.REGION }} --format='value(status.url)')
      #     curl -f "$URL/actuator/health" | jq .
      #     curl -f "$URL/hello" | jq .

      - name: Summary
        run: |
          echo "## Deploy Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** ${{ github.sha }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status:** GCP billing pending — deploy commented out" >> $GITHUB_STEP_SUMMARY
          echo "- **Image would be:** ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPOSITORY }}/${{ env.SERVICE }}:${{ github.sha }}" >> $GITHUB_STEP_SUMMARY